# Models

Large model binaries are ignored by git.

- gesture-cnn-baseline.keras: mandatory baseline
- gesture-cnn-lstm-comparison.keras: required comparison model for CNN vs CNN-LSTM
- model-metadata.json: labels, input shape, metrics, version and Cloud Storage URI
